export const environment = {
  production: true,
  // ✅ Utiliser le proxy Nginx (chemin relatif)
  apiUrl: '/api',
  socketUrl: '/',
  baseUrl: '',
  uploadUrl: '/uploads',
  
  isWeb: true,
  isReactNative: false,
  isCapacitor: false,
  version: '1.0.0',
};